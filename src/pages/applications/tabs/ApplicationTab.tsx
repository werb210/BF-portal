// BF_PORTAL_BLOCK_v174_APPLICATION_TAB_SEMANTIC_GROUPS_v1
import type { CSSProperties, ReactNode } from "react";
type AnyRecord = Record<string, any>;
type Props = { application: AnyRecord | null };
function pickPartner(...sources: any[]): AnyRecord | null { for (const s of sources) { if (s && typeof s === "object" && !Array.isArray(s)) return s as AnyRecord; } return null; }
export default function ApplicationTab({ application }: Props) {
  if (!application) return <div style={styles.page}><div style={styles.loading}>Loading application…</div></div>;
  const overview: AnyRecord = (application.overview as AnyRecord) ?? {};
  const business: AnyRecord = (application.businessDetails as AnyRecord) ?? (application.business as AnyRecord) ?? {};
  const applicant: AnyRecord = (application.applicantDetails as AnyRecord) ?? (application.applicantInfo as AnyRecord) ?? {};
  const fp: AnyRecord = (application.financialProfile as AnyRecord) ?? (application.kyc as AnyRecord) ?? {};
  const owners: AnyRecord[] = Array.isArray(application.owners) ? (application.owners as AnyRecord[]) : [];
  const raw: AnyRecord = (application.rawPayload as AnyRecord) ?? {};
  const fd: AnyRecord = (raw.formData && typeof raw.formData === "object" ? (raw.formData as AnyRecord) : {});
  const partner = pickPartner(raw.partner, raw.applicant ? raw.applicant.partner : null, fd.applicant ? fd.applicant.partner : null);
  const hasPartners = (raw.applicant && raw.applicant.hasPartners === true) || (fd.applicant && fd.applicant.hasPartners === true) || applicant.hasPartners === true || Boolean(partner);
  const businessName = fmt(business.legalName ?? business.name ?? application.applicant ?? "Unknown business");
  return <div style={styles.page}><div style={styles.headerRow}><div style={{ minWidth: 0 }}><h2 style={styles.title}>{businessName}</h2><div style={styles.subtitle}>{fmt(application.status, "—")} · Submitted {fmtDate(application.submittedAt)}</div></div><span style={styles.statusPill}>{fmt(application.stage, "—")}</span></div><SectionGroup title="Funding"><Field label="Requested Amount" value={fmtMoney(overview.requestedAmount ?? (application.fundingRequest as AnyRecord)?.amount)} highlight /><Field label="Use of Funds" value={fmt(fp.purposeOfFunds ?? fp.purpose ?? (application.fundingRequest as AnyRecord)?.purpose ?? raw.fundingRequest?.purpose)} /><Field label="Stage" value={fmt(application.stage)} /><Field label="Submitted" value={fmtDate(application.submittedAt)} /></SectionGroup><SectionGroup title="Product"><Field label="Product Category" value={fmt(overview.productCategory ?? application.productCategory)} /><Field label="Product Type" value={fmt(overview.productType)} /></SectionGroup><SectionGroup title="Business"><Field label="Legal Business Name" value={fmt(business.legalName ?? business.name)} /><Field label="Operating / DBA Name" value={fmt(business.operatingName ?? business.dba ?? business.dbaName)} /><Field label="Business Address" value={fmt(business.address ?? business.streetAddress ?? business.businessAddress)} /><Field label="City" value={fmt(business.city)} /><Field label="Province / State" value={fmt(business.state ?? business.province ?? business.region)} /><Field label="Postal / ZIP" value={fmt(business.postalCode ?? business.zip ?? business.zipCode)} /><Field label="Phone" value={fmt(business.phone ?? business.phoneNumber)} /><Field label="Email" value={business.email ? <Email value={String(business.email)} /> : "—"} /><Field label="Website" value={business.website ? <Anchor href={String(business.website)} /> : "—"} /><Field label="Industry" value={fmt(business.industry)} /><Field label="Entity Type" value={fmt(business.entityType ?? business.businessType)} /><Field label="Time in Business" value={fmt(business.timeInBusiness ?? business.yearsInBusiness)} /><Field label="Number of Employees" value={fmt(business.numberOfEmployees ?? business.employees)} /><Field label="Annual Revenue" value={fmtMoney(business.annualRevenue ?? fp.annualRevenue)} /><Field label="Monthly Revenue" value={fmtMoney(business.monthlyRevenue ?? fp.monthlyRevenue)} /><Field label="Tax ID / EIN" value={fmt(business.taxId ?? business.ein)} /></SectionGroup><SectionGroup title="Applicant"><Field label="Name" value={fmt(joinName(applicant) || "—")} /><Field label="Title" value={fmt(applicant.title)} /><Field label="Phone" value={fmt(applicant.phone ?? applicant.phoneNumber)} /><Field label="Email" value={applicant.email ? <Email value={String(applicant.email)} /> : "—"} /><Field label="Date of Birth" value={maskDob(applicant.dateOfBirth ?? applicant.dob)} /><Field label="SSN / SIN" value={maskSsn(applicant.ssn ?? applicant.sin ?? applicant.socialSecurityNumber)} /><Field label="Home Address" value={fmt(applicant.address ?? applicant.streetAddress ?? applicant.homeAddress)} /><Field label="City" value={fmt(applicant.city)} /><Field label="Province / State" value={fmt(applicant.state ?? applicant.province)} /><Field label="Postal / ZIP" value={fmt(applicant.postalCode ?? applicant.zip)} /><Field label="Credit Score" value={fmt(applicant.creditScore ?? fp.creditScore)} /></SectionGroup>{hasPartners && partner && <SectionGroup title="Partner"><Field label="Name" value={fmt(joinName(partner) || "—")} /><Field label="Title" value={fmt(partner.title)} /><Field label="Phone" value={fmt(partner.phone ?? partner.phoneNumber)} /><Field label="Email" value={partner.email ? <Email value={String(partner.email)} /> : "—"} /><Field label="Date of Birth" value={maskDob(partner.dateOfBirth ?? partner.dob)} /><Field label="SSN / SIN" value={maskSsn(partner.ssn ?? partner.sin)} /><Field label="Ownership %" value={fmtPercent(partner.ownershipPercent ?? partner.ownership)} /><Field label="Credit Score" value={fmt(partner.creditScore)} /></SectionGroup>}<SectionGroup title="Ownership">{owners.length === 0 ? <div style={styles.empty}>No ownership records on file.</div> : <div style={styles.ownerList}>{owners.map((o, i) => <div key={i} style={styles.ownerCard}><div style={styles.ownerHeader}>{joinName(o) || `Owner ${i + 1}`}</div><div style={styles.ownerFields}><CompactField label="Title" value={fmt(o.title)} /><CompactField label="Ownership %" value={fmtPercent(o.ownershipPercent ?? o.ownership)} /><CompactField label="Credit Score" value={fmt(o.creditScore)} /><CompactField label="DOB" value={maskDob(o.dateOfBirth ?? o.dob)} /><CompactField label="SSN / SIN" value={maskSsn(o.ssn ?? o.sin)} /></div></div>)}</div>}</SectionGroup><div style={styles.footer}>Submitted by the applicant. Read-only — to amend, request the client update via the mini-portal.</div></div>;
}
function joinName(p: any): string { if (!p || typeof p !== "object") return ""; const first = p.firstName ?? p.first_name ?? ""; const last = p.lastName ?? p.last_name ?? ""; const full = `${first} ${last}`.trim(); return full || p.fullName || p.name || ""; }
function fmt(v: unknown, fallback = "—"): string { if (v === null || v === undefined || v === "") return fallback; return String(v); }
function fmtMoney(v: unknown, fallback = "—"): string { let n = NaN; if (typeof v === "number") n = v; else if (typeof v === "string") n = Number(v.replace(/[^0-9.\-]/g, "")); return !Number.isFinite(n) || n <= 0 ? fallback : `$${Math.round(n).toLocaleString()}`; }
function fmtPercent(v: unknown, fallback = "—"): string { if (v === null || v === undefined || v === "") return fallback; const n = typeof v === "number" ? v : Number(String(v).replace(/[^0-9.\-]/g, "")); if (!Number.isFinite(n)) return fallback; return `${n}%`; }
function fmtDate(v: unknown, fallback = "—"): string { if (typeof v !== "string" || !v) return fallback; const d = new Date(v); return Number.isNaN(d.getTime()) ? fallback : d.toLocaleDateString("en-CA", { year: "numeric", month: "short", day: "numeric" }); }
function maskSsn(v: unknown, fallback = "—"): string { if (v === null || v === undefined || v === "") return fallback; const digits = String(v).replace(/\D/g, ""); if (digits.length === 0) return fallback; if (digits.length < 4) return "•".repeat(9); return `•••-••-${digits.slice(-4)}`; }
function maskDob(v: unknown, fallback = "—"): string { if (v === null || v === undefined || v === "") return fallback; const s = String(v); const d = new Date(s); if (!Number.isNaN(d.getTime())) { return `••/••/${d.getFullYear()}`; } const yr = s.match(/(\d{4})/); return yr ? `••/••/${yr[1]}` : "••/••/••••"; }
function SectionGroup({ title, children }: { title: string; children: ReactNode }) { return <section style={styles.sectionGroup}><h3 style={styles.sectionTitle}>{title}</h3><div style={styles.sectionGrid}>{children}</div></section>; }
function Field({ label, value, highlight }: { label: string; value: ReactNode; highlight?: boolean }) { return <div style={styles.field}><div style={styles.fieldLabel}>{label}</div><div style={highlight ? styles.fieldValueHighlight : styles.fieldValue}>{value}</div></div>; }
function CompactField({ label, value }: { label: string; value: ReactNode }) { return <div style={styles.compactField}><span style={styles.compactLabel}>{label}:</span><span style={styles.compactValue}>{value}</span></div>; }
function Email({ value }: { value: string }) { return <a href={`mailto:${value}`} style={styles.link}>{value}</a>; }
function Anchor({ href }: { href: string }) { const url = String(href).trim(); const display = url.replace(/^https?:\/\//i, ""); const safe = /^https?:\/\//i.test(url) ? url : `https://${url}`; return <a href={safe} target="_blank" rel="noreferrer" style={styles.link}>{display}</a>; }
const styles: Record<string, CSSProperties> = { page: { color: "#0f172a", maxWidth: 1200 }, loading: { padding: 40, textAlign: "center", color: "#64748b" }, headerRow: { display: "flex", justifyContent: "space-between", alignItems: "flex-start", marginBottom: 24, gap: 16, flexWrap: "wrap" }, title: { fontSize: 24, fontWeight: 700, margin: 0, color: "#0f172a", lineHeight: 1.2 }, subtitle: { fontSize: 13, color: "#64748b", marginTop: 4 }, statusPill: { background: "#dbeafe", color: "#1e40af", borderRadius: 999, padding: "4px 12px", fontSize: 12, fontWeight: 600, textTransform: "uppercase", letterSpacing: "0.04em", whiteSpace: "nowrap" }, sectionGroup: { background: "#fff", border: "1px solid #e5e7eb", borderRadius: 12, padding: "16px 20px", marginBottom: 16, boxShadow: "0 1px 2px rgba(15, 23, 42, 0.04)" }, sectionTitle: { fontSize: 12, fontWeight: 700, margin: "0 0 12px", textTransform: "uppercase", letterSpacing: "0.08em", color: "#475569", borderBottom: "1px solid #f1f5f9", paddingBottom: 8 }, sectionGrid: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))", gap: "14px 24px" }, field: { display: "flex", flexDirection: "column", gap: 2, minWidth: 0 }, fieldLabel: { fontSize: 11, fontWeight: 600, color: "#64748b", textTransform: "uppercase", letterSpacing: "0.04em" }, fieldValue: { fontSize: 14, color: "#0f172a", fontWeight: 500, overflow: "hidden", textOverflow: "ellipsis" }, fieldValueHighlight: { fontSize: 18, color: "#0f172a", fontWeight: 700 }, link: { color: "#2563eb", textDecoration: "none" }, ownerList: { display: "flex", flexDirection: "column", gap: 12, gridColumn: "1 / -1" }, ownerCard: { background: "#f8fafc", borderRadius: 8, padding: "12px 14px", border: "1px solid #e2e8f0" }, ownerHeader: { fontSize: 14, fontWeight: 700, marginBottom: 8, color: "#0f172a" }, ownerFields: { display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(180px, 1fr))", gap: "6px 16px" }, compactField: { display: "flex", gap: 6, fontSize: 13, alignItems: "baseline" }, compactLabel: { color: "#64748b", fontWeight: 600 }, compactValue: { color: "#0f172a", fontWeight: 500 }, empty: { color: "#94a3b8", fontSize: 14, fontStyle: "italic", gridColumn: "1 / -1" }, footer: { marginTop: 16, fontSize: 12, color: "#94a3b8", fontStyle: "italic", textAlign: "center" } };
